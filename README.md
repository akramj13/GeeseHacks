# GeeseHacks
GeeseHacks Project

Initialize Backend
```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

```bash
cd frontend && npm install
cd ..
cd backend && npm install
```